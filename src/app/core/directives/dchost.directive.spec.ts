import { DchostDirective } from './dchost.directive';

describe('DchostDirective', () => {
  it('should create an instance', () => {
    const directive = new DchostDirective();
    expect(directive).toBeTruthy();
  });
});
